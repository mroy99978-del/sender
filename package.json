{
  "name": "smtp-email-sender",
  "version": "1.0.0",
  "description": "Simple web app to send email via SMTP with custom sender name, sender email, and reply-to",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemailer": "^6.9.14",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
